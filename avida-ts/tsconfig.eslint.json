{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // ensure this tsconfig cannot accidentally be used for a build
    "noEmit": true
  },
  "include": [ "packages/**/*", "reset.d.ts", "*.config.ts", "*.config.js", "**/*.mjs"]
}
